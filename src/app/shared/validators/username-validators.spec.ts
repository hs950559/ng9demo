import { UsernameValidators } from './username-validators';

describe('UsernameValidators', () => {
  it('should create an instance', () => {
    expect(new UsernameValidators()).toBeTruthy();
  });
});
